<%= form_for(@ws_v1_post) do |f| %>
  <% if @ws_v1_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ws_v1_post.errors.count, "error") %> prohibited this ws_v1_post from being saved:</h2>

      <ul>
      <% @ws_v1_post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
